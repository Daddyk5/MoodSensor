buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.0"  // Update to a version that supports SDK 34
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.22"
    }
}
